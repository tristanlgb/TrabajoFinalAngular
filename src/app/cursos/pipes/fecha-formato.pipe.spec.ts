import { FechaFormatoPipe } from './fecha-formato.pipe';

describe('FechaFormatoPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaFormatoPipe();
    expect(pipe).toBeTruthy();
  });
});
