import { BooleanoAtextoPipe } from './booleano-atexto.pipe';

describe('BooleanoAtextoPipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanoAtextoPipe();
    expect(pipe).toBeTruthy();
  });
});
